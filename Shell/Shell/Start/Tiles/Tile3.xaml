<UserControl
    x:Class="Shell.Shell.Start.Tiles.Tile3"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shell.Shell.Start.Tiles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="96"
    d:DesignWidth="196">

    <Button IsHitTestVisible="False" Width="196" Height="96" Grid.Column="1" CornerRadius="5" Grid.RowSpan="2" Grid.Row="2" Grid.ColumnSpan="2" Padding="0,0,0,0">
        <Grid Width="196" Height="96">
            <Image Source="{Binding Icon}"
                                Width="48"
                                Height="48" />
            <TextBlock Text="{Binding DisplayName}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                FontSize="12" Margin="6,0,0,6" />
        </Grid>
    </Button>
</UserControl>
